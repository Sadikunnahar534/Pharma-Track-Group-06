CREATE DATABASE pharma_track;
USE pharma_track;

CREATE TABLE manufacturer (
    manufacturer_id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    contact VARCHAR(50),
    address VARCHAR(255)
);
